- hosts: localhost
  connection: local
  become: yes
  roles:
  - itcase.deploy
  vars:
    deploy_user: 'user'
    deploy_app_repo: 'https://github.com/petrikoz/itcase.deploy.git'
    deploy_app_ver: 'master'
